from typing import Dict

def readability_formula_estimator(_text: str) -> Dict:
    """Deprecated stub – feature removed.

    The enrichment worker used to derive a `difficulty_band` from FKGL.  We've
    retired that in favour of explicit CSV data.  This stub keeps import paths
    working while always returning an empty dict.
    """

    return {} 